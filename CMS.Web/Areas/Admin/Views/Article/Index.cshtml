@model IEnumerable<CMS.Models.Article.ArticleListModel>

@{
    ViewBag.Title = "Výpis textů";
}

<h2>@ViewData["Title"]</h2>

<table class="table table-striped">
    <thead>
    <tr>
        <th>Titulek</th>
        <th>Popis</th>
        <th></th>
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.Title</td>
            <td>@item.Description</td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Upravit</a> 
                @* @Html.DisplayText("|") *@
                @* <a asp-action="Details" asp-route-id="@item.Id">Detail</a> *@
                @* @Html.DisplayText("|") *@
                @* <a asp-action="Delete" asp-route-id="@item.Id">Delete</a> *@
            </td>
        </tr>
    }
    <br />
    <tr>
            <td>Video - hlavní stránka</td>
            <td></td>
            <td><a asp-controller="Video" asp-action="Edit" asp-area="Admin" asp-route-id="3274022f-a350-4d02-b753-2aafaedeffc1">Upravit</a></td>
        </tr>
    </tbody>
</table>